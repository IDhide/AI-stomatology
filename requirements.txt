# Smile.AI — веб-киоск (голос + тестовый DIKIDI). Один рабочий вариант.
# Всё pure-python, без torch/pygame/opencv.

loguru>=0.7.2
pyyaml>=6.0.1
pydantic>=2.5
python-dotenv>=1.0
aiohttp>=3.9
requests>=2.31

# Серверное распознавание речи (для Firefox) — опционально:
#   pip install -r requirements-stt.txt
# LLM-мозг работает через Ollama (внешний сервис), python-пакеты не нужны.
